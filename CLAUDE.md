# CLAUDE.md

Speciate uses a harness-agnostic **`AGENTS.md`** as the single source of truth for agent and contributor guidance. This file exists only so Claude Code auto-loads that guidance; everything lives in `AGENTS.md` (and the area-specific `AGENTS.md` files it points to).

@AGENTS.md
