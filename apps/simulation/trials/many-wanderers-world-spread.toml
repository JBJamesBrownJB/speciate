# Many wanderers
#
# Purpose: Create a large crowd of 10k wanderers
# This trial creates a dense grid of wandering creatures

name = "Many Wanderers Populated World"
description = """
"""

# Primary spawn pattern: 100×100 grid at world center
[[spawns]]
type = "grid"
start_x = -100000.0    # Center grid at (0, 0): starts at -25m to span [-25, 25]
start_y = -100000.0
spacing = 1000.0
rows = 200
cols = 200
creature_type = "wanderer"
grid_offset_y = 1.0  # Offset even columns by half spacing for staggered pattern