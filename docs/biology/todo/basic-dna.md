Just sets size and fov for now.

Code wise it should be its own component.

Must persist in save states.

Extend dev-ui so that when we spawn a crit we can choose its dna attributes.

attributes will have min/max, dev-ui will offer slider to pick between min / max.

We should add tiral capability to randomise dna on spawn, so we can spawn large numbers of variants in one go.

any questions? 

This is the first initiation of the dna concept, so its important its elegent architectural design for future.