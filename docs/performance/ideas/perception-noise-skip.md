Every crit may randomly skip a perception tick.

Would this smooth out any clustered perception ticks ?

Because at the moment, the skip tick code where a crit skips a perception tick if its neighbours are full, can cause latency jumps if lots of crits all in sync doing the same togehter. 

Would this help reduce clumping like this?