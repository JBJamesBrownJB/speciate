Use the biomass spatial grid when implimented to provide the visualisation for the minimap.

use a tiling algorithm on front end to show blobs joining in large blobs rather than just grid squares.

Add all appropriate dev overlays for the extra grid.